name: CI

on: [push]

jobs:
  generate-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Python 3.11
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'
          architecture: 'x64'

      - name: Install environment
        run: make environment/install
      
      - name: Generate
        run: make generate

      - name: Open generated project
        run: cd example_project

      - name: Install poetry
        run: make poetry

      - name: Install dependencies and dev dependencies
        run: make install-dev

      - name: Run tests
        run: make test
      
      - name: Run linter
        run: make lint